# inputs

Place the source Excel file in this folder before running the launcher.

Expected pattern:
- File extension: `.xlsx`
- File name contains: `範疇三業務管理報表`

Examples:
- `2026年_範疇三業務管理報表_20260514.xlsx`

Launch command from repository root:

```powershell
.\scope3-ghg.ps1 process
```
